<div class="container mx-auto sm:py-18 pb-10 px-2 relative z-10 flex flex-wrap items-center justify-between">
  <div class="w-full text-center my-10">
    <p class="text-indigo-600 font-medium mb-1 lg:mt-0 mt-2 dark:text-indigo-500">National and Club</p>
    <h1 class="sm:text-6xl text-4xl font-extrabold">Championships </h1>
  </div>

<div class="container mx-auto mt-8 px-4">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-4">
    <% @championships.each do |championship| %>
      <%= render 'championship_card', championship: championship %>
    <% end %>
  </div>
</div>